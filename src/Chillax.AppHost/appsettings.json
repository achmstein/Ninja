{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Aspire.Hosting.Dcp": "Warning"
    }
  },
  "AI": {
    // Any OpenAI-compatible provider: change these two and set the key in the
    // environment (GEMINI_API_KEY / OPENAI_API_KEY) or in the AppHost's user
    // secrets (Parameters:openai-openai-apikey). The key is never in this repo.
    "Endpoint": "https://generativelanguage.googleapis.com/v1beta/openai/",
    "ChatModel": "gemini-2.5-flash"
  }
}
