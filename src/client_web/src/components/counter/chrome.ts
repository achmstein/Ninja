/**
 * The Counter chrome's measures, shared by the bars and the menu under them.
 */

/** The top bar's height, px: the order pill (44 px, 10 px from the top) sits in its middle */
export const COUNTER_BAR_H = 64

/** Room at the top of the deck and the grid for the top bar over them, px */
export const DECK_TOP = COUNTER_BAR_H + 4

/** The tray's row of the bottom dock, px; the order sheet tucks under its top */
export const DOCK_H = 68

/** The dock's gap to the screen's edges, px */
export const DOCK_INSET = 8
