// This file is auto-generated by @hey-api/openapi-ts

import type { Client, ClientMeta, Options as Options2, RequestResult, TDataShape } from './client';
import { client } from './client.gen';
import type { AddCashMovementData, AddCashMovementErrors, AddCashMovementResponses, AddTicketLineData, AddTicketLineErrors, AddTicketLineResponses, ApplyTicketDiscountData, ApplyTicketDiscountErrors, ApplyTicketDiscountResponses, AssignTicketLinesCustomerData, AssignTicketLinesCustomerErrors, AssignTicketLinesCustomerResponses, CloseShiftData, CloseShiftErrors, CloseShiftResponses, DiscardTicketData, DiscardTicketErrors, DiscardTicketResponses, GetBranchPricingData, GetBranchPricingErrors, GetBranchPricingResponses, GetBreakdownReportData, GetBreakdownReportErrors, GetBreakdownReportResponses, GetClosedShiftsData, GetClosedShiftsErrors, GetClosedShiftsResponses, GetCurrentShiftData, GetCurrentShiftErrors, GetCurrentShiftResponses, GetMyBillsData, GetMyBillsErrors, GetMyBillsResponses, GetOpenTicketsData, GetOpenTicketsErrors, GetOpenTicketsResponses, GetPaymentsData, GetPaymentsErrors, GetPaymentsResponses, GetRangeReportData, GetRangeReportErrors, GetRangeReportResponses, GetRefundsData, GetRefundsErrors, GetRefundsResponses, GetSettledTicketsData, GetSettledTicketsErrors, GetSettledTicketsResponses, GetShiftData, GetShiftErrors, GetShiftResponses, GetTabPaymentData, GetTabPaymentErrors, GetTabPaymentResponses, GetTabPaymentsData, GetTabPaymentsErrors, GetTabPaymentsResponses, GetTicketByOrderData, GetTicketByOrderErrors, GetTicketByOrderResponses, GetTicketData, GetTicketErrors, GetTicketHistoryData, GetTicketHistoryErrors, GetTicketHistoryResponses, GetTicketReceiptData, GetTicketReceiptErrors, GetTicketReceiptResponses, GetTicketResponses, MoveTicketLinesData, MoveTicketLinesErrors, MoveTicketLinesResponses, OpenShiftData, OpenShiftErrors, OpenShiftResponses, OpenTicketData, OpenTicketErrors, OpenTicketResponses, RecordTabPaymentData, RecordTabPaymentErrors, RecordTabPaymentResponses, RefundTicketData, RefundTicketErrors, RefundTicketResponses, RemoveTicketDiscountData, RemoveTicketDiscountErrors, RemoveTicketDiscountResponses, SetBranchPricingData, SetBranchPricingErrors, SetBranchPricingResponses, SettleTicketData, SettleTicketErrors, SettleTicketResponses, VoidTicketData, VoidTicketErrors, VoidTicketResponses } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean, TResponse = unknown> = Options2<TData, ThrowOnError, TResponse> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: keyof ClientMeta extends never ? Record<string, unknown> : ClientMeta;
};

/**
 * Open tickets for the branch (the POS floor)
 */
export const getOpenTickets = <ThrowOnError extends boolean = false>(options: Options<GetOpenTicketsData, ThrowOnError>): RequestResult<GetOpenTicketsResponses, GetOpenTicketsErrors, ThrowOnError> => (options.client ?? client).get<GetOpenTicketsResponses, GetOpenTicketsErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/tickets/open',
    ...options
});

/**
 * Settled bills for the branch, newest receipt first
 *
 * The receipts screen: the way back to a bill after it closed, to reprint it or refund it. Pass receiptNumber to find one.
 */
export const getSettledTickets = <ThrowOnError extends boolean = false>(options: Options<GetSettledTicketsData, ThrowOnError>): RequestResult<GetSettledTicketsResponses, GetSettledTicketsErrors, ThrowOnError> => (options.client ?? client).get<GetSettledTicketsResponses, GetSettledTicketsErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/tickets/settled',
    ...options
});

/**
 * Closed tickets — settled or voided — newest first, with the count (the back office)
 *
 * The window is on the moment the ticket closed: settledAt for Settled, voidedAt for Voided. Pass receiptNumber to find one bill regardless of the window. Read-only; the till's own list is /settled.
 */
export const getTicketHistory = <ThrowOnError extends boolean = false>(options: Options<GetTicketHistoryData, ThrowOnError>): RequestResult<GetTicketHistoryResponses, GetTicketHistoryErrors, ThrowOnError> => (options.client ?? client).get<GetTicketHistoryResponses, GetTicketHistoryErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/tickets/history',
    ...options
});

/**
 * Payments taken on tickets settled in a window, newest first
 *
 * Windowed on the ticket's settle, not the payment, so the page adds up to the range report's tender split for the same window. Pass tender to see one kind.
 */
export const getPayments = <ThrowOnError extends boolean = false>(options: Options<GetPaymentsData, ThrowOnError>): RequestResult<GetPaymentsResponses, GetPaymentsErrors, ThrowOnError> => (options.client ?? client).get<GetPaymentsResponses, GetPaymentsErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/tickets/payments',
    ...options
});

/**
 * Credit notes issued in a window, newest first
 *
 * Every refund across the branch's tickets; open the ticket for the lines behind one.
 */
export const getRefunds = <ThrowOnError extends boolean = false>(options: Options<GetRefundsData, ThrowOnError>): RequestResult<GetRefundsResponses, GetRefundsErrors, ThrowOnError> => (options.client ?? client).get<GetRefundsResponses, GetRefundsErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/tickets/refunds',
    ...options
});

/**
 * Tab payment slips taken in a window, newest first
 *
 * Money customers handed the till against their tabs. Not sales: the bills were counted when they went on account.
 */
export const getTabPayments = <ThrowOnError extends boolean = false>(options: Options<GetTabPaymentsData, ThrowOnError>): RequestResult<GetTabPaymentsResponses, GetTabPaymentsErrors, ThrowOnError> => (options.client ?? client).get<GetTabPaymentsResponses, GetTabPaymentsErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/tickets/tab-payments',
    ...options
});

/**
 * Take money against a customer's tab
 *
 * Cash into the drawer, card or InstaPay to the terminal — never Account. A numbered slip, stamped with the branch's open shift so the drawer count and the Z report include it; Accounts lowers the balance owed off the event. Slips are never voided: a mistake is reversed by a manual charge on the ledger (and a cash pay-out if cash was handed back).
 */
export const recordTabPayment = <ThrowOnError extends boolean = false>(options: Options<RecordTabPaymentData, ThrowOnError>): RequestResult<RecordTabPaymentResponses, RecordTabPaymentErrors, ThrowOnError> => (options.client ?? client).post<RecordTabPaymentResponses, RecordTabPaymentErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/tickets/tab-payments',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * One tab payment slip, to reprint it
 */
export const getTabPayment = <ThrowOnError extends boolean = false>(options: Options<GetTabPaymentData, ThrowOnError>): RequestResult<GetTabPaymentResponses, GetTabPaymentErrors, ThrowOnError> => (options.client ?? client).get<GetTabPaymentResponses, GetTabPaymentErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/tickets/tab-payments/{id}',
    ...options
});

/**
 * Discard an empty open ticket
 *
 * Opened by mistake and never used. Counter and table tickets with no lines only — nothing happened on them, so there is nothing to audit and the row is deleted. A ticket with lines is voided (owner) instead.
 */
export const discardTicket = <ThrowOnError extends boolean = false>(options: Options<DiscardTicketData, ThrowOnError>): RequestResult<DiscardTicketResponses, DiscardTicketErrors, ThrowOnError> => (options.client ?? client).delete<DiscardTicketResponses, DiscardTicketErrors, ThrowOnError>({ url: '/api/tickets/{id}', ...options });

/**
 * Ticket detail with lines, payments and receipt number
 */
export const getTicket = <ThrowOnError extends boolean = false>(options: Options<GetTicketData, ThrowOnError>): RequestResult<GetTicketResponses, GetTicketErrors, ThrowOnError> => (options.client ?? client).get<GetTicketResponses, GetTicketErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/tickets/{id}',
    ...options
});

/**
 * Settled sales over a window: tender split, discounts, per-type counts
 *
 * The caller picks the window — pass the branch's business-day bounds for the day report.
 */
export const getRangeReport = <ThrowOnError extends boolean = false>(options: Options<GetRangeReportData, ThrowOnError>): RequestResult<GetRangeReportResponses, GetRangeReportErrors, ThrowOnError> => (options.client ?? client).get<GetRangeReportResponses, GetRangeReportErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/tickets/reports/range',
    ...options
});

/**
 * Settled sales in a window by hour, weekday, cashier and item
 *
 * offsetMinutes is how far the caller's clock is ahead of UTC; hours and weekdays come back in it.
 */
export const getBreakdownReport = <ThrowOnError extends boolean = false>(options: Options<GetBreakdownReportData, ThrowOnError>): RequestResult<GetBreakdownReportResponses, GetBreakdownReportErrors, ThrowOnError> => (options.client ?? client).get<GetBreakdownReportResponses, GetBreakdownReportErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/tickets/reports/breakdown',
    ...options
});

/**
 * The ticket a confirmed order landed on
 *
 * 404 while the order's confirmation event is still in flight — the POS polls this after creating a counter sale.
 */
export const getTicketByOrder = <ThrowOnError extends boolean = false>(options: Options<GetTicketByOrderData, ThrowOnError>): RequestResult<GetTicketByOrderResponses, GetTicketByOrderErrors, ThrowOnError> => (options.client ?? client).get<GetTicketByOrderResponses, GetTicketByOrderErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/tickets/by-order/{orderId}',
    ...options
});

/**
 * Open a counter or table ticket by hand
 *
 * Room tickets are never opened this way — they follow their session events.
 */
export const openTicket = <ThrowOnError extends boolean = false>(options: Options<OpenTicketData, ThrowOnError>): RequestResult<OpenTicketResponses, OpenTicketErrors, ThrowOnError> => (options.client ?? client).post<OpenTicketResponses, OpenTicketErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/tickets',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Add a manual line to an open ticket
 */
export const addTicketLine = <ThrowOnError extends boolean = false>(options: Options<AddTicketLineData, ThrowOnError>): RequestResult<AddTicketLineResponses, AddTicketLineErrors, ThrowOnError> => (options.client ?? client).post<AddTicketLineResponses, AddTicketLineErrors, ThrowOnError>({
    url: '/api/tickets/{id}/lines',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Settle an open ticket
 *
 * Payments must cover the total; on cash, the excess is returned as change. Issues the receipt number.
 */
export const settleTicket = <ThrowOnError extends boolean = false>(options: Options<SettleTicketData, ThrowOnError>): RequestResult<SettleTicketResponses, SettleTicketErrors, ThrowOnError> => (options.client ?? client).post<SettleTicketResponses, SettleTicketErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/tickets/{id}/settle',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Move lines to another ticket
 *
 * No target: the table-turnover split, a fresh ticket for the same place. A target ticket: onto that open bill — the customer who ordered at a table and then took a room. A new ticket: a fresh counter tab, or a table's bill (opened if the table has none) — the customer who moved tables or went to pay at the counter. Session time never moves; a table or counter ticket left empty is discarded. Returns the ticket the lines ended up on.
 */
export const moveTicketLines = <ThrowOnError extends boolean = false>(options: Options<MoveTicketLinesData, ThrowOnError>): RequestResult<MoveTicketLinesResponses, MoveTicketLinesErrors, ThrowOnError> => (options.client ?? client).post<MoveTicketLinesResponses, MoveTicketLinesErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/tickets/{id}/move-lines',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Name the customer on chosen lines
 *
 * The split-bill fix: several people rang up as one sale, and some lines were theirs. Sets the customer snapshot on just those lines — bill grouping, receipt, Account tender. Points do not move: they follow the whole order (assign the order's customer in Ordering for that). Session time cannot be reassigned.
 */
export const assignTicketLinesCustomer = <ThrowOnError extends boolean = false>(options: Options<AssignTicketLinesCustomerData, ThrowOnError>): RequestResult<AssignTicketLinesCustomerResponses, AssignTicketLinesCustomerErrors, ThrowOnError> => (options.client ?? client).post<AssignTicketLinesCustomerResponses, AssignTicketLinesCustomerErrors, ThrowOnError>({
    url: '/api/tickets/{id}/lines/customer',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Take the discount back off an open ticket
 */
export const removeTicketDiscount = <ThrowOnError extends boolean = false>(options: Options<RemoveTicketDiscountData, ThrowOnError>): RequestResult<RemoveTicketDiscountResponses, RemoveTicketDiscountErrors, ThrowOnError> => (options.client ?? client).delete<RemoveTicketDiscountResponses, RemoveTicketDiscountErrors, ThrowOnError>({ url: '/api/tickets/{id}/discount', ...options });

/**
 * Take a percent or an amount off the whole bill, with a reason
 *
 * Exactly one of rate (a fraction, 0.1 is 10%) or amount; the reason is optional. A cashier is capped by the branch's MaxCashierDiscountRate; an owner is not. Given again, it replaces the earlier discount.
 */
export const applyTicketDiscount = <ThrowOnError extends boolean = false>(options: Options<ApplyTicketDiscountData, ThrowOnError>): RequestResult<ApplyTicketDiscountResponses, ApplyTicketDiscountErrors, ThrowOnError> => (options.client ?? client).post<ApplyTicketDiscountResponses, ApplyTicketDiscountErrors, ThrowOnError>({
    url: '/api/tickets/{id}/discount',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Void an open ticket with nothing owed (owner only)
 *
 * A mistake, a comp, a walked group. The reason is the audit trail. Settled tickets cannot be voided.
 */
export const voidTicket = <ThrowOnError extends boolean = false>(options: Options<VoidTicketData, ThrowOnError>): RequestResult<VoidTicketResponses, VoidTicketErrors, ThrowOnError> => (options.client ?? client).post<VoidTicketResponses, VoidTicketErrors, ThrowOnError>({
    url: '/api/tickets/{id}/void',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Refund lines of a settled ticket as a numbered credit note (owner only)
 *
 * Full or partial, by line and quantity. Each line gives back what the customer paid for it, service charge and VAT included. Cash comes out of the drawer; Account credits the named tab. Loyalty points the refunded orders earned are clawed back in proportion. The settled ticket itself never changes.
 */
export const refundTicket = <ThrowOnError extends boolean = false>(options: Options<RefundTicketData, ThrowOnError>): RequestResult<RefundTicketResponses, RefundTicketErrors, ThrowOnError> => (options.client ?? client).post<RefundTicketResponses, RefundTicketErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/tickets/{id}/refunds',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * How a branch's menu prices become the bill: VAT, whether it sits inside the price, service charge
 */
export const getBranchPricing = <ThrowOnError extends boolean = false>(options: Options<GetBranchPricingData, ThrowOnError>): RequestResult<GetBranchPricingResponses, GetBranchPricingErrors, ThrowOnError> => (options.client ?? client).get<GetBranchPricingResponses, GetBranchPricingErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/tickets/pricing/{branchId}',
    ...options
});

/**
 * Set a branch's VAT and service charge (owner only)
 *
 * Rates are fractions: 0.14 is 14%. Service applies to what is ordered at tables and rooms, never to counter sales or room time. Applies to tickets settled from now on; printed receipts keep their figures.
 */
export const setBranchPricing = <ThrowOnError extends boolean = false>(options: Options<SetBranchPricingData, ThrowOnError>): RequestResult<SetBranchPricingResponses, SetBranchPricingErrors, ThrowOnError> => (options.client ?? client).put<SetBranchPricingResponses, SetBranchPricingErrors, ThrowOnError>({
    url: '/api/tickets/pricing/{branchId}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * The caller's bills: open ones, and those settled since a given time
 *
 * Every ticket the caller is on — sat in the room, ordered a line, or paid a share — with its lines and the till's totals. Open bills always; settled and voided ones since `since` (default: the last 24 hours). Signed in, or a guest by X-Guest-Id.
 */
export const getMyBills = <ThrowOnError extends boolean = false>(options: Options<GetMyBillsData, ThrowOnError>): RequestResult<GetMyBillsResponses, GetMyBillsErrors, ThrowOnError> => (options.client ?? client).get<GetMyBillsResponses, GetMyBillsErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/tickets/mine',
    ...options
});

/**
 * A settled bill's receipt, for a customer who was on it
 *
 * The customer's own copy of the printed receipt. Allowed for whoever sat in the room, ordered a line, or paid a share; 404 until the bill is settled.
 */
export const getTicketReceipt = <ThrowOnError extends boolean = false>(options: Options<GetTicketReceiptData, ThrowOnError>): RequestResult<GetTicketReceiptResponses, GetTicketReceiptErrors, ThrowOnError> => (options.client ?? client).get<GetTicketReceiptResponses, GetTicketReceiptErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/tickets/{id}/receipt',
    ...options
});

/**
 * Open the branch's drawer shift with a counted float
 */
export const openShift = <ThrowOnError extends boolean = false>(options: Options<OpenShiftData, ThrowOnError>): RequestResult<OpenShiftResponses, OpenShiftErrors, ThrowOnError> => (options.client ?? client).post<OpenShiftResponses, OpenShiftErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/shifts/open',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * The branch's open shift as a live X report (404 when none)
 */
export const getCurrentShift = <ThrowOnError extends boolean = false>(options: Options<GetCurrentShiftData, ThrowOnError>): RequestResult<GetCurrentShiftResponses, GetCurrentShiftErrors, ThrowOnError> => (options.client ?? client).get<GetCurrentShiftResponses, GetCurrentShiftErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/shifts/current',
    ...options
});

/**
 * One shift as an X (open) or Z (closed) report
 */
export const getShift = <ThrowOnError extends boolean = false>(options: Options<GetShiftData, ThrowOnError>): RequestResult<GetShiftResponses, GetShiftErrors, ThrowOnError> => (options.client ?? client).get<GetShiftResponses, GetShiftErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/shifts/{id}',
    ...options
});

/**
 * Closed shifts, newest first — the Z-report history
 */
export const getClosedShifts = <ThrowOnError extends boolean = false>(options: Options<GetClosedShiftsData, ThrowOnError>): RequestResult<GetClosedShiftsResponses, GetClosedShiftsErrors, ThrowOnError> => (options.client ?? client).get<GetClosedShiftsResponses, GetClosedShiftsErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/shifts',
    ...options
});

/**
 * Record cash paid into or out of the drawer
 */
export const addCashMovement = <ThrowOnError extends boolean = false>(options: Options<AddCashMovementData, ThrowOnError>): RequestResult<AddCashMovementResponses, AddCashMovementErrors, ThrowOnError> => (options.client ?? client).post<AddCashMovementResponses, AddCashMovementErrors, ThrowOnError>({
    url: '/api/shifts/{id}/movements',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Count the drawer and close the shift
 *
 * Freezes expected cash and over/short; returns the Z report.
 */
export const closeShift = <ThrowOnError extends boolean = false>(options: Options<CloseShiftData, ThrowOnError>): RequestResult<CloseShiftResponses, CloseShiftErrors, ThrowOnError> => (options.client ?? client).post<CloseShiftResponses, CloseShiftErrors, ThrowOnError>({
    responseType: 'json',
    url: '/api/shifts/{id}/close',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});
