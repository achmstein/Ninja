// TicketType enum values as Sales.Domain numbers them: Room=0, Table=1,
// Counter=2. The API takes the number; the read models spell the name.
export const TICKET_TYPE_ROOM = 0
export const TICKET_TYPE_TABLE = 1
export const TICKET_TYPE_COUNTER = 2
