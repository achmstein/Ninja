// This file is auto-generated by @hey-api/openapi-ts

import { type DefaultError, queryOptions, type UseMutationOptions } from '@tanstack/react-query';
import type { AxiosError } from 'axios';

import { client } from '../client.gen';
import { addCashMovement, addTicketLine, applyTicketDiscount, assignTicketLinesCustomer, closeShift, discardTicket, getBillToPay, getBranchPricing, getBreakdownReport, getClosedShifts, getCurrentShift, getMyBills, getOnlinePayment, getOpenTickets, getPayments, getPaymentSettings, getPlaceBillToPay, getRangeReport, getRefunds, getSettledTickets, getShift, getTabPayment, getTabPayments, getTicket, getTicketByOrder, getTicketHistory, getTicketReceipt, listOnlinePayments, moveTicketLines, openShift, openTicket, type Options, recordTabPayment, refundOnlinePayment, refundTicket, removeTicketDiscount, savePaymentSettings, setBranchPricing, settleTicket, startOnlinePayment, voidTicket } from '../sdk.gen';
import type { AddCashMovementData, AddCashMovementError, AddTicketLineData, AddTicketLineError, ApplyTicketDiscountData, ApplyTicketDiscountError, AssignTicketLinesCustomerData, AssignTicketLinesCustomerError, AssignTicketLinesCustomerResponse, CloseShiftData, CloseShiftError, CloseShiftResponse, DiscardTicketData, DiscardTicketError, DiscardTicketResponse, GetBillToPayData, GetBillToPayResponse, GetBranchPricingData, GetBranchPricingResponse, GetBreakdownReportData, GetBreakdownReportError, GetBreakdownReportResponse, GetClosedShiftsData, GetClosedShiftsResponse, GetCurrentShiftData, GetCurrentShiftResponse, GetMyBillsData, GetMyBillsResponse, GetOnlinePaymentData, GetOnlinePaymentResponse, GetOpenTicketsData, GetOpenTicketsResponse, GetPaymentsData, GetPaymentsError, GetPaymentSettingsData, GetPaymentSettingsResponse, GetPaymentsResponse, GetPlaceBillToPayData, GetPlaceBillToPayResponse, GetRangeReportData, GetRangeReportError, GetRangeReportResponse, GetRefundsData, GetRefundsError, GetRefundsResponse, GetSettledTicketsData, GetSettledTicketsResponse, GetShiftData, GetShiftResponse, GetTabPaymentData, GetTabPaymentResponse, GetTabPaymentsData, GetTabPaymentsError, GetTabPaymentsResponse, GetTicketByOrderData, GetTicketByOrderResponse, GetTicketData, GetTicketHistoryData, GetTicketHistoryError, GetTicketHistoryResponse, GetTicketReceiptData, GetTicketReceiptResponse, GetTicketResponse, ListOnlinePaymentsData, ListOnlinePaymentsResponse, MoveTicketLinesData, MoveTicketLinesError, MoveTicketLinesResponse, OpenShiftData, OpenShiftError, OpenShiftResponse2, OpenTicketData, OpenTicketError, OpenTicketResponse2, RecordTabPaymentData, RecordTabPaymentError, RecordTabPaymentResponse, RefundOnlinePaymentData, RefundOnlinePaymentError, RefundOnlinePaymentResponse, RefundTicketData, RefundTicketError, RefundTicketResponse, RemoveTicketDiscountData, RemoveTicketDiscountError, RemoveTicketDiscountResponse, SavePaymentSettingsData, SavePaymentSettingsError, SavePaymentSettingsResponse, SetBranchPricingData, SetBranchPricingError, SettleTicketData, SettleTicketError, SettleTicketResponse, StartOnlinePaymentData, StartOnlinePaymentError, StartOnlinePaymentResponse, VoidTicketData, VoidTicketError } from '../types.gen';

export type QueryKey<TOptions extends Options> = [
    Pick<TOptions, 'baseURL' | 'body' | 'headers' | 'path' | 'query'> & {
        _id: string;
        _infinite?: boolean;
        tags?: ReadonlyArray<string>;
    }
];

const createQueryKey = <TOptions extends Options>(id: string, options?: TOptions, infinite?: boolean, tags?: ReadonlyArray<string>): [
    QueryKey<TOptions>[0]
] => {
    const params: QueryKey<TOptions>[0] = { _id: id, baseURL: options?.baseURL || (options?.client ?? client).getConfig().baseURL } as QueryKey<TOptions>[0];
    if (infinite) {
        params._infinite = infinite;
    }
    if (tags) {
        params.tags = tags;
    }
    if (options?.body) {
        params.body = options.body;
    }
    if (options?.headers) {
        params.headers = options.headers;
    }
    if (options?.path) {
        params.path = options.path;
    }
    if (options?.query) {
        params.query = options.query;
    }
    return [params];
};

export const getOpenTicketsQueryKey = (options: Options<GetOpenTicketsData>) => createQueryKey('getOpenTickets', options);

/**
 * Open tickets for the branch (the POS floor)
 */
export const getOpenTicketsOptions = (options: Options<GetOpenTicketsData>) => queryOptions<GetOpenTicketsResponse, AxiosError<DefaultError>, GetOpenTicketsResponse, ReturnType<typeof getOpenTicketsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getOpenTickets({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getOpenTicketsQueryKey(options)
});

export const getSettledTicketsQueryKey = (options: Options<GetSettledTicketsData>) => createQueryKey('getSettledTickets', options);

/**
 * Settled bills for the branch, newest receipt first
 *
 * The receipts screen: the way back to a bill after it closed, to reprint it or refund it. Pass receiptNumber to find one.
 */
export const getSettledTicketsOptions = (options: Options<GetSettledTicketsData>) => queryOptions<GetSettledTicketsResponse, AxiosError<DefaultError>, GetSettledTicketsResponse, ReturnType<typeof getSettledTicketsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getSettledTickets({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getSettledTicketsQueryKey(options)
});

export const getTicketHistoryQueryKey = (options: Options<GetTicketHistoryData>) => createQueryKey('getTicketHistory', options);

/**
 * Closed tickets — settled or voided — newest first, with the count (the back office)
 *
 * The window is on the moment the ticket closed: settledAt for Settled, voidedAt for Voided. Pass receiptNumber to find one bill regardless of the window. Read-only; the till's own list is /settled.
 */
export const getTicketHistoryOptions = (options: Options<GetTicketHistoryData>) => queryOptions<GetTicketHistoryResponse, AxiosError<GetTicketHistoryError>, GetTicketHistoryResponse, ReturnType<typeof getTicketHistoryQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getTicketHistory({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getTicketHistoryQueryKey(options)
});

export const getPaymentsQueryKey = (options: Options<GetPaymentsData>) => createQueryKey('getPayments', options);

/**
 * Payments taken on tickets settled in a window, newest first
 *
 * Windowed on the ticket's settle, not the payment, so the page adds up to the range report's tender split for the same window. Pass tender to see one kind.
 */
export const getPaymentsOptions = (options: Options<GetPaymentsData>) => queryOptions<GetPaymentsResponse, AxiosError<GetPaymentsError>, GetPaymentsResponse, ReturnType<typeof getPaymentsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getPayments({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getPaymentsQueryKey(options)
});

export const getRefundsQueryKey = (options: Options<GetRefundsData>) => createQueryKey('getRefunds', options);

/**
 * Credit notes issued in a window, newest first
 *
 * Every refund across the branch's tickets; open the ticket for the lines behind one.
 */
export const getRefundsOptions = (options: Options<GetRefundsData>) => queryOptions<GetRefundsResponse, AxiosError<GetRefundsError>, GetRefundsResponse, ReturnType<typeof getRefundsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getRefunds({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getRefundsQueryKey(options)
});

export const getTabPaymentsQueryKey = (options: Options<GetTabPaymentsData>) => createQueryKey('getTabPayments', options);

/**
 * Tab payment slips taken in a window, newest first
 *
 * Money customers handed the till against their tabs. Not sales: the bills were counted when they went on account.
 */
export const getTabPaymentsOptions = (options: Options<GetTabPaymentsData>) => queryOptions<GetTabPaymentsResponse, AxiosError<GetTabPaymentsError>, GetTabPaymentsResponse, ReturnType<typeof getTabPaymentsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getTabPayments({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getTabPaymentsQueryKey(options)
});

/**
 * Take money against a customer's tab
 *
 * Cash into the drawer, card or InstaPay to the terminal — never Account. A numbered slip, stamped with the branch's open shift so the drawer count and the Z report include it; Accounts lowers the balance owed off the event. Slips are never voided: a mistake is reversed by a manual charge on the ledger (and a cash pay-out if cash was handed back).
 */
export const recordTabPaymentMutation = (options?: Partial<Options<RecordTabPaymentData>>): UseMutationOptions<RecordTabPaymentResponse, AxiosError<RecordTabPaymentError>, Options<RecordTabPaymentData>> => {
    const mutationOptions: UseMutationOptions<RecordTabPaymentResponse, AxiosError<RecordTabPaymentError>, Options<RecordTabPaymentData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await recordTabPayment({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const getTabPaymentQueryKey = (options: Options<GetTabPaymentData>) => createQueryKey('getTabPayment', options);

/**
 * One tab payment slip, to reprint it
 */
export const getTabPaymentOptions = (options: Options<GetTabPaymentData>) => queryOptions<GetTabPaymentResponse, AxiosError<DefaultError>, GetTabPaymentResponse, ReturnType<typeof getTabPaymentQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getTabPayment({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getTabPaymentQueryKey(options)
});

/**
 * Discard an empty open ticket
 *
 * Opened by mistake and never used. Counter and table tickets with no lines only — nothing happened on them, so there is nothing to audit and the row is deleted. A ticket with lines is voided (owner) instead.
 */
export const discardTicketMutation = (options?: Partial<Options<DiscardTicketData>>): UseMutationOptions<DiscardTicketResponse, AxiosError<DiscardTicketError>, Options<DiscardTicketData>> => {
    const mutationOptions: UseMutationOptions<DiscardTicketResponse, AxiosError<DiscardTicketError>, Options<DiscardTicketData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await discardTicket({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const getTicketQueryKey = (options: Options<GetTicketData>) => createQueryKey('getTicket', options);

/**
 * Ticket detail with lines, payments and receipt number
 */
export const getTicketOptions = (options: Options<GetTicketData>) => queryOptions<GetTicketResponse, AxiosError<DefaultError>, GetTicketResponse, ReturnType<typeof getTicketQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getTicket({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getTicketQueryKey(options)
});

export const getRangeReportQueryKey = (options: Options<GetRangeReportData>) => createQueryKey('getRangeReport', options);

/**
 * Settled sales over a window: tender split, discounts, per-type counts
 *
 * The caller picks the window — pass the branch's business-day bounds for the day report.
 */
export const getRangeReportOptions = (options: Options<GetRangeReportData>) => queryOptions<GetRangeReportResponse, AxiosError<GetRangeReportError>, GetRangeReportResponse, ReturnType<typeof getRangeReportQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getRangeReport({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getRangeReportQueryKey(options)
});

export const getBreakdownReportQueryKey = (options: Options<GetBreakdownReportData>) => createQueryKey('getBreakdownReport', options);

/**
 * Settled sales in a window by hour, weekday, cashier and item
 *
 * offsetMinutes is how far the caller's clock is ahead of UTC; hours and weekdays come back in it.
 */
export const getBreakdownReportOptions = (options: Options<GetBreakdownReportData>) => queryOptions<GetBreakdownReportResponse, AxiosError<GetBreakdownReportError>, GetBreakdownReportResponse, ReturnType<typeof getBreakdownReportQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getBreakdownReport({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getBreakdownReportQueryKey(options)
});

export const getTicketByOrderQueryKey = (options: Options<GetTicketByOrderData>) => createQueryKey('getTicketByOrder', options);

/**
 * The ticket a confirmed order landed on
 *
 * 404 while the order's confirmation event is still in flight — the POS polls this after creating a counter sale.
 */
export const getTicketByOrderOptions = (options: Options<GetTicketByOrderData>) => queryOptions<GetTicketByOrderResponse, AxiosError<DefaultError>, GetTicketByOrderResponse, ReturnType<typeof getTicketByOrderQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getTicketByOrder({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getTicketByOrderQueryKey(options)
});

/**
 * Open a counter or table ticket by hand
 *
 * Room tickets are never opened this way — they follow their session events.
 */
export const openTicketMutation = (options?: Partial<Options<OpenTicketData>>): UseMutationOptions<OpenTicketResponse2, AxiosError<OpenTicketError>, Options<OpenTicketData>> => {
    const mutationOptions: UseMutationOptions<OpenTicketResponse2, AxiosError<OpenTicketError>, Options<OpenTicketData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await openTicket({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Add a manual line to an open ticket
 */
export const addTicketLineMutation = (options?: Partial<Options<AddTicketLineData>>): UseMutationOptions<unknown, AxiosError<AddTicketLineError>, Options<AddTicketLineData>> => {
    const mutationOptions: UseMutationOptions<unknown, AxiosError<AddTicketLineError>, Options<AddTicketLineData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await addTicketLine({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Settle an open ticket
 *
 * Payments must cover the total; on cash, the excess is returned as change. Issues the receipt number.
 */
export const settleTicketMutation = (options?: Partial<Options<SettleTicketData>>): UseMutationOptions<SettleTicketResponse, AxiosError<SettleTicketError>, Options<SettleTicketData>> => {
    const mutationOptions: UseMutationOptions<SettleTicketResponse, AxiosError<SettleTicketError>, Options<SettleTicketData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await settleTicket({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Move lines to another ticket
 *
 * No target: the table-turnover split, a fresh ticket for the same place. A target ticket: onto that open bill — the customer who ordered at a table and then took a room. A new ticket: a fresh counter tab, or a table's bill (opened if the table has none) — the customer who moved tables or went to pay at the counter. Session time never moves; a table or counter ticket left empty is discarded. Returns the ticket the lines ended up on.
 */
export const moveTicketLinesMutation = (options?: Partial<Options<MoveTicketLinesData>>): UseMutationOptions<MoveTicketLinesResponse, AxiosError<MoveTicketLinesError>, Options<MoveTicketLinesData>> => {
    const mutationOptions: UseMutationOptions<MoveTicketLinesResponse, AxiosError<MoveTicketLinesError>, Options<MoveTicketLinesData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await moveTicketLines({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Name the customer on chosen lines
 *
 * The split-bill fix: several people rang up as one sale, and some lines were theirs. Sets the customer snapshot on just those lines — bill grouping, receipt, Account tender. Points do not move: they follow the whole order (assign the order's customer in Ordering for that). Session time cannot be reassigned.
 */
export const assignTicketLinesCustomerMutation = (options?: Partial<Options<AssignTicketLinesCustomerData>>): UseMutationOptions<AssignTicketLinesCustomerResponse, AxiosError<AssignTicketLinesCustomerError>, Options<AssignTicketLinesCustomerData>> => {
    const mutationOptions: UseMutationOptions<AssignTicketLinesCustomerResponse, AxiosError<AssignTicketLinesCustomerError>, Options<AssignTicketLinesCustomerData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await assignTicketLinesCustomer({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Take the discount back off an open ticket
 */
export const removeTicketDiscountMutation = (options?: Partial<Options<RemoveTicketDiscountData>>): UseMutationOptions<RemoveTicketDiscountResponse, AxiosError<RemoveTicketDiscountError>, Options<RemoveTicketDiscountData>> => {
    const mutationOptions: UseMutationOptions<RemoveTicketDiscountResponse, AxiosError<RemoveTicketDiscountError>, Options<RemoveTicketDiscountData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await removeTicketDiscount({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Take a percent or an amount off the whole bill, with a reason
 *
 * Exactly one of rate (a fraction, 0.1 is 10%) or amount; the reason is optional. A cashier is capped by the branch's MaxCashierDiscountRate; an owner is not. Given again, it replaces the earlier discount.
 */
export const applyTicketDiscountMutation = (options?: Partial<Options<ApplyTicketDiscountData>>): UseMutationOptions<unknown, AxiosError<ApplyTicketDiscountError>, Options<ApplyTicketDiscountData>> => {
    const mutationOptions: UseMutationOptions<unknown, AxiosError<ApplyTicketDiscountError>, Options<ApplyTicketDiscountData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await applyTicketDiscount({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Void an open ticket with nothing owed (owner only)
 *
 * A mistake, a comp, a walked group. The reason is the audit trail. Settled tickets cannot be voided.
 */
export const voidTicketMutation = (options?: Partial<Options<VoidTicketData>>): UseMutationOptions<unknown, AxiosError<VoidTicketError>, Options<VoidTicketData>> => {
    const mutationOptions: UseMutationOptions<unknown, AxiosError<VoidTicketError>, Options<VoidTicketData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await voidTicket({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Refund lines of a settled ticket as a numbered credit note (owner only)
 *
 * Full or partial, by line and quantity. Each line gives back what the customer paid for it, service charge and VAT included. Cash comes out of the drawer; Account credits the named tab. Loyalty points the refunded orders earned are clawed back in proportion. The settled ticket itself never changes.
 */
export const refundTicketMutation = (options?: Partial<Options<RefundTicketData>>): UseMutationOptions<RefundTicketResponse, AxiosError<RefundTicketError>, Options<RefundTicketData>> => {
    const mutationOptions: UseMutationOptions<RefundTicketResponse, AxiosError<RefundTicketError>, Options<RefundTicketData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await refundTicket({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const getBranchPricingQueryKey = (options: Options<GetBranchPricingData>) => createQueryKey('getBranchPricing', options);

/**
 * How a branch's menu prices become the bill: VAT, whether it sits inside the price, service charge
 */
export const getBranchPricingOptions = (options: Options<GetBranchPricingData>) => queryOptions<GetBranchPricingResponse, AxiosError<DefaultError>, GetBranchPricingResponse, ReturnType<typeof getBranchPricingQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getBranchPricing({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getBranchPricingQueryKey(options)
});

/**
 * Set a branch's VAT and service charge (owner only)
 *
 * Rates are fractions: 0.14 is 14%. Service applies to what is ordered at tables and rooms, never to counter sales or room time. Applies to tickets settled from now on; printed receipts keep their figures.
 */
export const setBranchPricingMutation = (options?: Partial<Options<SetBranchPricingData>>): UseMutationOptions<unknown, AxiosError<SetBranchPricingError>, Options<SetBranchPricingData>> => {
    const mutationOptions: UseMutationOptions<unknown, AxiosError<SetBranchPricingError>, Options<SetBranchPricingData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await setBranchPricing({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const getMyBillsQueryKey = (options: Options<GetMyBillsData>) => createQueryKey('getMyBills', options);

/**
 * The caller's bills: open ones, and those settled since a given time
 *
 * Every ticket the caller is on — sat in the room, ordered a line, or paid a share — with its lines and the till's totals. Open bills always; settled and voided ones since `since` (default: the last 24 hours). Signed in, or a guest by X-Guest-Id.
 */
export const getMyBillsOptions = (options: Options<GetMyBillsData>) => queryOptions<GetMyBillsResponse, AxiosError<DefaultError>, GetMyBillsResponse, ReturnType<typeof getMyBillsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getMyBills({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getMyBillsQueryKey(options)
});

export const getTicketReceiptQueryKey = (options: Options<GetTicketReceiptData>) => createQueryKey('getTicketReceipt', options);

/**
 * A settled bill's receipt, for a customer who was on it
 *
 * The customer's own copy of the printed receipt. Allowed for whoever sat in the room, ordered a line, or paid a share; 404 until the bill is settled.
 */
export const getTicketReceiptOptions = (options: Options<GetTicketReceiptData>) => queryOptions<GetTicketReceiptResponse, AxiosError<DefaultError>, GetTicketReceiptResponse, ReturnType<typeof getTicketReceiptQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getTicketReceipt({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getTicketReceiptQueryKey(options)
});

/**
 * Open the branch's drawer shift with a counted float
 */
export const openShiftMutation = (options?: Partial<Options<OpenShiftData>>): UseMutationOptions<OpenShiftResponse2, AxiosError<OpenShiftError>, Options<OpenShiftData>> => {
    const mutationOptions: UseMutationOptions<OpenShiftResponse2, AxiosError<OpenShiftError>, Options<OpenShiftData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await openShift({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const getCurrentShiftQueryKey = (options: Options<GetCurrentShiftData>) => createQueryKey('getCurrentShift', options);

/**
 * The branch's open shift as a live X report (404 when none)
 */
export const getCurrentShiftOptions = (options: Options<GetCurrentShiftData>) => queryOptions<GetCurrentShiftResponse, AxiosError<DefaultError>, GetCurrentShiftResponse, ReturnType<typeof getCurrentShiftQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getCurrentShift({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getCurrentShiftQueryKey(options)
});

export const getShiftQueryKey = (options: Options<GetShiftData>) => createQueryKey('getShift', options);

/**
 * One shift as an X (open) or Z (closed) report
 */
export const getShiftOptions = (options: Options<GetShiftData>) => queryOptions<GetShiftResponse, AxiosError<DefaultError>, GetShiftResponse, ReturnType<typeof getShiftQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getShift({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getShiftQueryKey(options)
});

export const getClosedShiftsQueryKey = (options: Options<GetClosedShiftsData>) => createQueryKey('getClosedShifts', options);

/**
 * Closed shifts, newest first — the Z-report history
 */
export const getClosedShiftsOptions = (options: Options<GetClosedShiftsData>) => queryOptions<GetClosedShiftsResponse, AxiosError<DefaultError>, GetClosedShiftsResponse, ReturnType<typeof getClosedShiftsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getClosedShifts({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getClosedShiftsQueryKey(options)
});

/**
 * Record cash paid into or out of the drawer
 */
export const addCashMovementMutation = (options?: Partial<Options<AddCashMovementData>>): UseMutationOptions<unknown, AxiosError<AddCashMovementError>, Options<AddCashMovementData>> => {
    const mutationOptions: UseMutationOptions<unknown, AxiosError<AddCashMovementError>, Options<AddCashMovementData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await addCashMovement({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Count the drawer and close the shift
 *
 * Freezes expected cash and over/short; returns the Z report.
 */
export const closeShiftMutation = (options?: Partial<Options<CloseShiftData>>): UseMutationOptions<CloseShiftResponse, AxiosError<CloseShiftError>, Options<CloseShiftData>> => {
    const mutationOptions: UseMutationOptions<CloseShiftResponse, AxiosError<CloseShiftError>, Options<CloseShiftData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await closeShift({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const getPlaceBillToPayQueryKey = (options: Options<GetPlaceBillToPayData>) => createQueryKey('getPlaceBillToPay', options);

/**
 * The open bill at a table, as a guest pays it
 *
 * For whoever is at the table: its lines with each one's share of the total and whether someone has paid for it, what is paid, held and left, and how the café lets guests split. 404 when nothing is open there.
 */
export const getPlaceBillToPayOptions = (options: Options<GetPlaceBillToPayData>) => queryOptions<GetPlaceBillToPayResponse, AxiosError<DefaultError>, GetPlaceBillToPayResponse, ReturnType<typeof getPlaceBillToPayQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getPlaceBillToPay({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getPlaceBillToPayQueryKey(options)
});

export const getBillToPayQueryKey = (options: Options<GetBillToPayData>) => createQueryKey('getBillToPay', options);

/**
 * A bill the caller is on, as they pay it
 */
export const getBillToPayOptions = (options: Options<GetBillToPayData>) => queryOptions<GetBillToPayResponse, AxiosError<DefaultError>, GetBillToPayResponse, ReturnType<typeof getBillToPayQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getBillToPay({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getBillToPayQueryKey(options)
});

/**
 * Start paying a share of a bill; answers where to send the guest to pay
 *
 * Full: what is left. Items: the lines picked. Equal: parts of N. Custom: an amount up to what is left. The share is held for 15 minutes while the guest is at the provider's checkout.
 */
export const startOnlinePaymentMutation = (options?: Partial<Options<StartOnlinePaymentData>>): UseMutationOptions<StartOnlinePaymentResponse, AxiosError<StartOnlinePaymentError>, Options<StartOnlinePaymentData>> => {
    const mutationOptions: UseMutationOptions<StartOnlinePaymentResponse, AxiosError<StartOnlinePaymentError>, Options<StartOnlinePaymentData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await startOnlinePayment({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const getOnlinePaymentQueryKey = (options: Options<GetOnlinePaymentData>) => createQueryKey('getOnlinePayment', options);

/**
 * How a payment stands, for the page the guest comes back to
 *
 * Only the provider's signed callback changes it; the guest's return proves nothing.
 */
export const getOnlinePaymentOptions = (options: Options<GetOnlinePaymentData>) => queryOptions<GetOnlinePaymentResponse, AxiosError<DefaultError>, GetOnlinePaymentResponse, ReturnType<typeof getOnlinePaymentQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getOnlinePayment({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getOnlinePaymentQueryKey(options)
});

export const listOnlinePaymentsQueryKey = (options: Options<ListOnlinePaymentsData>) => createQueryKey('listOnlinePayments', options);

/**
 * Online payments on a bill, for the till
 */
export const listOnlinePaymentsOptions = (options: Options<ListOnlinePaymentsData>) => queryOptions<ListOnlinePaymentsResponse, AxiosError<DefaultError>, ListOnlinePaymentsResponse, ReturnType<typeof listOnlinePaymentsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await listOnlinePayments({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: listOnlinePaymentsQueryKey(options)
});

/**
 * Give an online payment back through the provider, while its bill is open
 */
export const refundOnlinePaymentMutation = (options?: Partial<Options<RefundOnlinePaymentData>>): UseMutationOptions<RefundOnlinePaymentResponse, AxiosError<RefundOnlinePaymentError>, Options<RefundOnlinePaymentData>> => {
    const mutationOptions: UseMutationOptions<RefundOnlinePaymentResponse, AxiosError<RefundOnlinePaymentError>, Options<RefundOnlinePaymentData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await refundOnlinePayment({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const getPaymentSettingsQueryKey = (options: Options<GetPaymentSettingsData>) => createQueryKey('getPaymentSettings', options);

/**
 * How the café takes payments at the table; secrets only as whether they are set
 */
export const getPaymentSettingsOptions = (options: Options<GetPaymentSettingsData>) => queryOptions<GetPaymentSettingsResponse, AxiosError<DefaultError>, GetPaymentSettingsResponse, ReturnType<typeof getPaymentSettingsQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getPaymentSettings({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getPaymentSettingsQueryKey(options)
});

/**
 * Change the café's payment account, fee, tips and split options
 */
export const savePaymentSettingsMutation = (options?: Partial<Options<SavePaymentSettingsData>>): UseMutationOptions<SavePaymentSettingsResponse, AxiosError<SavePaymentSettingsError>, Options<SavePaymentSettingsData>> => {
    const mutationOptions: UseMutationOptions<SavePaymentSettingsResponse, AxiosError<SavePaymentSettingsError>, Options<SavePaymentSettingsData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await savePaymentSettings({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};
