# Chillax — handoff, 2026‑09‑16

Written for continuing on another PC. Everything below is **uncommitted on the first PC** (see §0 first). A copy of this file is in the repo at `docs/handoff-2026-09-16.md` so it travels with the push.

---

## 0. Before leaving this PC — the work is not in git yet

The whole day's work (~170 files) is on the working tree of `C:\Users\Admin\source\repos\achmstein\Chillax`, branch `main`, on top of `c19fb9c`. Nothing has been committed. On another PC none of it exists until it is pushed.

```powershell
cd C:\Users\Admin\source\repos\achmstein\Chillax
git add -A
git commit -m "Bill discounts, sales breakdown, receipt header, scheduled offers; admin_web minimal-UI pass rounds 1-2"
git push
```

If you prefer four feature commits + one UI commit, ask Claude on the other PC to split by the file lists in §1 — but push first; a single commit is fine.

Also not on git: Claude's memory for this project lives in `C:\Users\Admin\.claude\projects\C--Users-Admin-source-repos-achmstein-Chillax\memory\`. §4 of this file restates every rule and gotcha from it, so the other PC's Claude has what it needs. Copy that folder over too if you want the full history.

**Verification status:** every piece builds; unit tests pass (Sales 66/66, Catalog 37 + 5 skipped live tests); `tsc` and eslint clean on admin_web, pos_web, client_web; `flutter analyze` clean on pos_app; admin_web production build passes. **Nothing has been run end‑to‑end** — the AppHost was never started today. Four migrations apply automatically on startup: `TicketDiscount` (Sales), `BranchReceiptHeader` (Branch), `ItemOfferSchedule` (Catalog). A smoke run before deploy is owed.

---

## 1. What got added

### 1.1 Bill discounts (Sales + both tills + admin)
- `Ticket.ApplyDiscount(rate | amount, reason, by, maxRate)` / `RemoveDiscount()`; one discount per ticket; percent follows the lines while open, fixed amount capped by the bill; taken off the menu subtotal **before** service and VAT (service shrinks proportionally). `Bill.Discount`; settle freezes it.
- Policy: `BranchPricing.MaxCashierDiscountRate` (default 10 %), edited in admin's branch pricing dialog ("Cashier discount up to %"). Owner uncapped (`IsInRole("Owner")` in `TicketsApi`).
- API: `POST /api/tickets/{id}/discount` `{reason, rate?|amount?}` (Pos policy, idempotent), `DELETE /api/tickets/{id}/discount`. `TicketDetail` exposes `discount, discountRate, discountReason, discountBy`; `PricingView.maxCashierDiscountRate`; `ShiftView.discounts` and `RangeReport.discounts` include bill discounts.
- Refunds needed no change (they return `Total/Subtotal` per menu pound; `Subtotal` stays pre‑discount menu money). Finance P&L and Loyalty clawback unaffected.
- UI: `pos_web/src/features/ticket/discount-dialog.tsx`, `pos_app/lib/features/ticket/dialogs/discount_dialog.dart` — value, `%`/`EGP` toggle, reason, Cancel/Apply, Remove when one is on. Discount button on the open‑ticket header; discount on the bottom‑bar breakdown, the receipt, the Z report; admin ticket sheet row.
- Files: `Sales.Domain/…/Ticket.cs, PricingRules.cs, BranchPricing.cs`; `Sales.API/Apis/TicketsApi.cs`, `Application/Commands/ApplyTicketDiscountCommand.cs, SetBranchPricingCommand.cs`, `Queries/*`; `Sales.Infrastructure/EntityConfigurations/*`, migration `20260915230437_TicketDiscount`; `tests/Sales.UnitTests/Domain/TicketDiscountTest.cs`; pos_web `ticket/index.tsx, receipt/receipt-sheet.tsx, shift/shift-report.tsx, lib/i18n.ts`; pos_app `tickets/models/ticket_detail.dart, services/tickets_service.dart, core/demo/demo.dart, ticket/screens/ticket_screen.dart, shifts/*`, `l10n/*.arb`; admin `branches/components/pricing-dialog.tsx, till/components/ticket-sheet.tsx`.

### 1.2 Sales breakdown (Sales + admin)
- `GET /api/tickets/reports/breakdown?from&to&offsetMinutes` → by hour (24), by weekday (7) in the caller's clock, by cashier (tickets, net, discounts, voids, refunds — voids/refunds attributed to who issued them), by item (line name snapshot, qty, tickets, amount, top 50, negative lines excluded).
- admin_web: **Till → Breakdown** tab (`features/till/breakdown.tsx`, route `routes/_authenticated/till/breakdown.tsx`), bars as divs, two tables. No chart lib.
- Not included: by category — ticket lines carry no catalog id. Adding `CatalogItemId` to `TicketLine` from the confirmation event would enable it.

### 1.3 Receipt header per branch (Branch.API + admin + both tills)
- `Branch.TaxNumber`, `Branch.ReceiptFooter` (LocalizedText), on `PUT /api/branches/{id}`; migration `20260915233255_BranchReceiptHeader`. Admin branch dialog: Tax number + Receipt footer (EN/AR).
- Both tills print name, address, phone, tax number under the wordmark and the footer instead of "Thank you!" when set; branch read off the list the switcher already holds. pos_app receipt also gained the discount row (parity).

### 1.4 Scheduled offers (Catalog + admin; clients follow automatically)
- `CatalogItem.OfferWeekdays` (bit per DayOfWeek, none = every day), `OfferFrom/OfferTo` (local time, both null = all day; ending before starting runs past midnight and belongs to the day it started). `Catalog.API/Model/OfferWindow.cs`: `LocalClock` (Cairo; same choice as Finance's `BusinessDay`) + `OfferWindow.Covers`.
- `IsOfferActive` → `EffectivePrice` and the DTO's `IsOnOffer` mean "active now"; admin edits the configured switch via `Base`. Branch overrides only switch + price; the window is the item's. Set via `PUT /items/{id}` and `PATCH /items/{id}/offer`. Migration `20260915234602_ItemOfferSchedule`. `tests/Catalog.UnitTests/Model/OfferWindowTest.cs`.
- Admin item form: weekday chips + from–to under the offer switch (`menu/components/item-details-form.tsx`).

### 1.5 admin_web minimal‑UI pass, rounds 1–2
- New primitives: `components/info-tip.tsx` (ⓘ popover — the only home for explanations), `components/section.tsx` (shared Section, no hint slot). `ConfirmDialog.desc` now optional. `PageHeader` unchanged but no page passes a sentence.
- Removed: 16 page subtitles, ~28 dialog/sheet/card descriptions that restated the title, 10 empty‑state second lines, 5 switch captions, 4 section hints, 4 paragraph `title=` tooltips, the announcements Card, rooms' confirm bodies, the attendance footer paragraph. 92 unused i18n keys deleted from `lib/i18n.ts`.
- Relocated behind ⓘ: attendance legend/rules, recipe editor + builder rules, paid‑days‑off rule, prime‑cost formula, auto‑sold‑out, owner role, pay‑changed.
- Shortened confirm texts: retire item, rebuild levels, delete payslip, stop tracking; "Not tracked", "Empty = off".
- Type scale by element: h1 `text-2xl font-bold tracking-tight`, h2 `text-sm font-semibold`, h3/h4 `text-sm font-medium` — normalised across 21 files.
- Tables (`components/ui/table.tsx`, `components/data-table/*`): sticky header, uppercase muted xs header, `px-3 py-2.5` cells, `rounded-lg`; `columnMeta.align: 'end'` (end + tabular) on 31 money/number columns; `columnMeta.emphasis: 'primary'|'muted'`.
- Fixed: pre‑existing `react-hooks/set-state-in-effect` in `pricing-dialog.tsx` (form mounts from loaded data).

### 1.6 Documents
- `docs/ui-minimal-review.md` — the three audits (admin_web, tills+KDS, customer apps): per‑screen tables, top‑15 deletions each, the repeating patterns, and the 8 rules.
- `docs/pos-plan.md` — four new entries in Phase 4.
- Plan file from the gap analysis (not in repo): Batch 1 = the four features above (done); Batch 2 = owner picks one of prepaid packages / online payments (Paymob/Fawry) / WhatsApp / promo codes.

---

## 2. The plan — what is left, in order

### 2.1 admin_web, round 3 — surfaces & density ("premium back‑office")

**Done 2026‑09‑16 (second PC):** `Main` owns the page rhythm (`gap-6`, no per‑page classes); sheets are `SheetContent` width only + `SheetHeader` (hairline) + `SheetBody`/`Section`s + `SheetFooter`; the last two card grids (inventory reports, menu cost) and the shift report’s three heroes are `StatStrip`s; stat hints are bare numbers; header actions are `size="sm"` and the six hand‑rolled `<h1>` pages use `PageHeader`; surfaces are `rounded-lg border` with no shadow; dialog widths sit on the Tailwind scale; error pages are a title only; the four leftover page subtitles and 13 unused keys are gone. Not done: the dead ARB keys in §2.1.8 are all still used by `admin_app`, so they stay.
Judge in the browser (`cd src/admin_web && npm run dev`). Fix primitives, not pages:
1. `components/layout/main.tsx`: one vertical rhythm — page gap `gap-6`, section gap `gap-3`; audit pages using `gap-8`.
2. Sheets: three widths are in use (`sm:max-w-lg | xl | 3xl`) — keep, but make padding/header consistent (`SheetHeader` + `Section`s, no ad‑hoc `p-4` blocks).
3. `Stat`/`StatStrip`: one size for strip cells, `hero` for the page number; hints only counts.
4. Buttons: page actions `size='sm'` in headers; forms `default`; no `h-12` outside the tills.
5. Borders/radius: `rounded-lg border` everywhere a surface is drawn; no `shadow` except popovers.
6. Empty states: icon + one line + action (already), check compact variants in panels.
7. Remaining prose per `docs/ui-minimal-review.md` §3 per‑page table (e.g. error pages' sentences live in the generated ARB — edit `src/admin_app/lib/l10n/app_en.arb` + `app_ar.arb`, then `npm run generate:i18n`).
8. Delete dead ARB keys listed in the review (`batteryOptimizationBody`, `fullScreenIntentBody`, block/unblock confirmations…) if `admin_app` doesn't use them either.

### 2.2 pos_web + pos_app (one list, two files each — zero wording drift)

**Done 2026‑09‑16 (second PC):** `ConfirmDialog.description` / `showConfirmDialog(description:)` optional; the three settle/void guards, mode switch, walk‑in start, new‑tab, discard and cancel‑reservation bodies are gone; `refundHint` → "{amount} left", `cancelSessionHint` → "No charge.", `endSessionBilledAt` → "{hours} on the bill.", `noBranchDescription` → "Ask the owner to assign a branch.", `accessDeniedDescription` → "No access.", `cancelOrderConfirm` → "Cancel order?"; `voidReasonHint`, `takingAutoHint`, `typeToSearch`, `emptySale`, both empty‑state second lines and the four pay‑out "add them in the admin app" lines (now one `none`) are gone; pos_app settings captions sit behind an `InfoTip` (new `core/widgets/info_tip.dart`), the offline chip is its title only; the ticket bar shows the total and a "Breakdown" button opening label/value rows (`breakdown-dialog.tsx` / `breakdown_dialog.dart`, refunded‑so‑far included); the voided block and credit notes are bordered label/value rows; the 7 dead pos_app keys are deleted. 21 web keys and 28 app keys removed, `breakdown` + `none` added on both. Tables were already end‑aligned + tabular on both tills.
From `docs/ui-minimal-review.md` §1:
1. `pos_web/src/components/confirm-dialog.tsx` and `pos_app/lib/core/widgets/confirm_dialog.dart`: make `description` optional → removes 6 of the top 15 at once.
2. Delete the top‑15 keys in `pos_web/src/lib/i18n.ts` ⇄ `pos_app/lib/l10n/app_en.arb` + `app_ar.arb` (kiosk/printer/offline hints → info icons; session/pending guard hints; refund hint → "{amount} left"; discard hint; start‑walk‑in description; cancel‑session hint → "No charge."; branch gate → "Ask the owner to assign a branch."; new‑tab hint). Then `flutter gen-l10n` in pos_app.
3. Dialog headers: title only (new‑ticket, discard, refund, move‑target, start‑session, shift‑panel, order‑detail, customize).
4. Empty states: one line (delete every `*Hint` pair).
5. Ticket bottom bar: Total only, breakdown behind a disclosure; refund cards / voided block as label/value rows.
6. pos_app settings: three `FCard(subtitle:)` paragraphs → info icons.
7. Dead pos_app keys: `cancelOrderConfirmation, cancelOrderQuestion, noKeep, yesCancel, comingSoon, each, priceFormat`.
8. Tables/lists on the till: same alignment rule (numbers end, tabular) in `shift-report`, `receipts`, `history`.

### 2.3 kds_web + kds_app
Cut `noOrdersHint`, `historyHint`, the sound `Alert` → speaker icon button, `kioskHint` → info icon, gate strings as POS.

### 2.4 client_web + client_app (`docs/ui-minimal-review.md` §2)
1. Delete the 22 dead ARB keys + two unrouted screens (`update_email_screen.dart`, `change_password_screen.dart`).
2. Top‑15 deletions in `src/client_app/lib/l10n/app_en.arb` + `app_ar.arb`, then `npm run generate:i18n` in client_web; remove the orphaned render sites the compilers point at.
3. Confirm dialogs → title + Cancel/Verb (kill "No, Keep" / "Yes, Cancel"); empty states → one line.
4. Port web `ThemeSwitch` and the one "Call Us" tile into the Flutter settings/profile.
5. Notice boxes → chips/disabled states; sentence‑values ("Total: X") → label/value rows; delete the web toast title layer (`client_web/src/lib/toast.ts`).

### 2.5 Then
- Smoke‑run everything with the AppHost; deploy (create nothing new on prod: the migrations run on startup).
- Batch 2 — owner decision: prepaid packages / online payments / WhatsApp / promo codes.

---

## 3. How to continue with Claude on the other PC

Open the repo and paste this (adjust as needed):

> Read `docs/handoff-2026-09-16.md` and `docs/ui-minimal-review.md`. Continue the minimal‑UI pass at §2.1 (admin_web round 3), following the rules in §4. Work one surface at a time, verify with tsc/eslint (SPAs) and `flutter analyze` (apps), and give me a summary after each surface.

---

## 4. Rules and gotchas (restated from Claude's memory)

**Owner rules**
- Minimal UI, far less text: label + value + control; no helper sentences; **progressive disclosure** — every explanation is one tap away (ⓘ, expand, hover), never resident. Dialog = title + inputs + one action. Empty state = one line + action. Confirm = question in the title, Cancel/Verb, at most "Cannot be undone."
- One type scale per element on every page; tables readable (numbers end‑aligned + tabular, key column emphasised, muted secondaries, sticky header, status as colour/badge). Target: a premium, calm back‑office.
- Everything in both apps: pos_web ⇄ pos_app parity every batch (and admin where relevant).
- No dated/calendar room bookings, ever (rooms are walk‑in + 10‑minute hold). No table reservations or waitlists.
- Flat one‑click sidebar; no card grids in admin_web (StatStrip, one surface per band); real shadcn components.
- HARD RULE: services never call each other synchronously — events + local read models only; the SPA/BFF may join.
- Deliberate and simple: settle design decisions first, build the minimal thing; best practices over shortcuts.

**Tooling gotchas**
- Bash tool: long python heredocs with unicode/backslashes fail to parse — write the script to a file and run it.
- `dotnet ef migrations add … --no-build` uses the *last built* assembly: build the project first or the migration comes out empty. Sales migrations: `--project src/Sales.Infrastructure --startup-project src/Sales.API`; Branch/Catalog: `--project src/<Svc>.API`.
- OpenAPI json regenerates at `dotnet build`; then `npm run generate:api` in each SPA. It rewrites *other* services' clients with EOL‑only noise — `git checkout` any `src/api/<svc>` dir with no `git diff -w` change.
- Tests use Microsoft.Testing.Platform: `dotnet test` rejects `--filter`/`-nologo`.
- Unit‑test `TicketLine`s have Id 0 — subclass `TicketLine` to set an Id when a test moves lines.
- Forui: `FButton` default variant is `null` (there is no `.primary`); `FButton.raw` truncates; Riverpod here has no `valueOrNull` (use `.value`).
- pos_app `l10n.yaml` + `flutter gen-l10n`; client/admin web i18n is generated from the sibling Flutter ARB (`npm run generate:i18n`); pos_web `i18n.ts` is hand‑written.
- admin_web routes: adding a file under `src/routes` needs `npx vite build` (or dev) to regenerate `routeTree.gen.ts` before `tsc` passes; `vitest` type errors in `tsc -b` are pre‑existing (dev dep missing).
- TanStack column meta: `meta: { align: 'end' }` for numbers; `className/thClassName/tdClassName` for widths.
- `IsInRole` on `ClaimsPrincipal` is the ServiceDefaults extension (reads `role` claims); Owner policy = `RequireRole("Owner")`.
- Prod: postgres data is in the named volume; never prune the old anonymous volume; the classifier blocks prod writes — owner runs scripts via `!`.
